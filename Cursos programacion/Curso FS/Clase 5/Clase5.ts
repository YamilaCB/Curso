/* 
Modularizacion y metodos:
cuando devemos repetir codigo, creamos una funcion que despues puede ser invocada, por ejemplo:
function dibujarGuion   (creamos el codigo para la funcion)
y luego para llamar la funcion:
dibujarGuion ();

la funcion se puede declarar de 3 formas:
como sentencia, como valor de una variable, como arrow function

la funcion puede tener un valor modificable que se define dentro de esta cuando la declaramos, por ejemplo:
dibujarGuion (30);
*/